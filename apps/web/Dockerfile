# ─── Stage 1: Prune monorepo to only what gym-app needs ───────────────────────
FROM oven/bun:1.3.9-alpine AS pruner
WORKDIR /app

RUN bun add -g turbo@^2

COPY . .
RUN turbo prune gym-app --docker

# ─── Stage 2: Install pruned dependencies ─────────────────────────────────────
FROM oven/bun:1.3.9-alpine AS installer
WORKDIR /app

# Ensure build-time devDependencies (vite, plugins, etc.) are installed even if
# the CI environment sets NODE_ENV=production.
ENV NODE_ENV=development

# Copy pruned package manifests and lockfile (generated by turbo prune --docker)
COPY --from=pruner /app/out/json/ .
COPY --from=pruner /app/out/bun.lock ./bun.lock

RUN bun install --frozen-lockfile || bun install

# ─── Stage 3: Build ───────────────────────────────────────────────────────────
FROM oven/bun:1.3.9-alpine AS builder
WORKDIR /app

COPY --from=pruner /app/out/bun.lock ./bun.lock
COPY --from=installer /app/node_modules ./node_modules
COPY --from=pruner /app/out/full/ .

# Re-install in the builder layer to avoid broken links/binaries when copying
# Bun-managed node_modules across stages.
RUN bun install --frozen-lockfile || bun install

# Build-time env vars (injected by Dokploy / docker build --build-arg)
ARG VITE_CONVEX_URL
ENV VITE_CONVEX_URL=${VITE_CONVEX_URL}

ARG VITE_CONVEX_SITE_URL
ENV VITE_CONVEX_SITE_URL=${VITE_CONVEX_SITE_URL}

RUN bun turbo run build --filter=gym-app

# ─── Stage 4: Production runner ───────────────────────────────────────────────
FROM oven/bun:1.3.9-alpine AS runner
WORKDIR /app

ENV NODE_ENV=production
ENV PORT=3000

# VITE_CONVEX_URL and VITE_CONVEX_SITE_URL are checked at SSR runtime.
ARG VITE_CONVEX_URL
ENV VITE_CONVEX_URL=${VITE_CONVEX_URL}

ARG VITE_CONVEX_SITE_URL
ENV VITE_CONVEX_SITE_URL=${VITE_CONVEX_SITE_URL}

# Nitro "bun" preset emits a self-contained .output directory
COPY --from=builder /app/apps/web/.output ./.output

EXPOSE 3000

HEALTHCHECK --interval=30s --timeout=5s --start-period=20s --retries=3 \
	CMD bun -e "fetch('http://127.0.0.1:3000/health').then((r) => r.ok ? process.exit(0) : process.exit(1)).catch(() => process.exit(1))"

CMD ["bun", ".output/server/index.mjs"]